@objects
    login-box           id  hdtb-msb


= Login box =
    @on *
        login-box:
            height 50 to 70px

    @on desktop, tablet
        login-box:
            height 50 to 70px

    @on mobile
        login-box:
            height 50 to 70px

